#include "list.h"

/*
 * Checks if a permutation of a given board state is contained in cache.
 * Returns true if a hit.
 * Returns false, and add the permutation to cache if miss.
 * */
bool cache_lookup_permutations(struct LList *cache, struct Board *board);



